main: function integer () = {
    a: integer = 10;
    b: integer = 20;
    
    c: auto = (a + b) * (a - b) / 2 % 3;
    d: integer = c + 5;  
    
    e: auto = a < b && (a + 5) > 3;
    f: boolean = e || true;  
    
    return 0;
}

main1: function integer () = {
    a: integer = 1;
    b: integer = 2;
    c: integer = 3;
    d: integer = 4;
    
    // Deeply nested expression with correct types
    result: boolean = ((a + b) * (c - d) / 2) < ((a * b) + (c * d)) && 
                      (!(a > b) || (c <= d)) && 
                      ((a ^ 2) + (b ^ 2)) == ((c ^ 2) + (d ^ 2));
    
    return 0;
}